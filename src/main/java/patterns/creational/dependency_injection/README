Внедрение зависимостей.
Например, мы имеем сервис-класс А, который для своей работы использует одину из реализаций другого сервис-класса Б1, Б2, Б3.
Используется аггрегация - присвоение извне, например, через конструктор, сеттер.
В класс А мы внедряем через конструктор интерфейс Б и нам не важно какая именно реализация будет.
Мы работаем с интерфейсом, а конкретная реализация будет передана нам в класс А через конструктор.